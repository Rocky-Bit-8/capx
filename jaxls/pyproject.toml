[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jaxls"
version = "0.0.0"
description = "Local shim for jaxls used by PyRoki"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["jaxls"]
